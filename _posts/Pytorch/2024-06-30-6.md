---
title:  "[PyTorch] torch.nn.LSTM 관련 공식문서 정리"
excerpt: "torch.nn.LSTM"

categories:
  - Pytorch
tags:
  - [torch.nn, LSTM]
toc: true
toc_sticky: true
 
date: 2024-06-30
last_modified_at: 2024-06-30
---

# LSTM
> CLASS torch.nn.LSTM(input_size, hidden_size, num_layers=1, bias=True, batch_first=False, dropout=0.0, bidirectional=False, proj_size=0, device=None, dtype=None)

다층 LSTM(장단기 메모리 순환 신경망)을 입력 시퀀스에 적용합니다. 각 계층은 입력 시퀀스의 각 요소에 대해 다음 함수를 계산합니다.

$$i_t = \sigma(W_{ii} x_t + b_{ii} + W_{hi} h_{t-1} + b_{hi})$$ <br>
$$f_t = \sigma(W_{if} x_t + b_{if} + W_{hf} h_{t-1} + b_{hf})$$ <br>
$$g_t = \tanh(W_{ig} x_t + b_{ig} + W_{hg} h_{t-1} + b_{hg})$$ <br>
$$o_t = \sigma(W_{io} x_t + b_{io} + W_{ho} h_{t-1} + b_{ho})$$ <br>
$$c_t = f_t \odot c_{t-1} + i_t \odot g_t$$ <br>
$$h_t = o_t \odot \tanh(c_t)$$

자세한 내용은 논문을 참조하시면 될 것 같습니다. [https://arxiv.org/abs/1402.1128](https://arxiv.org/abs/1402.1128)

## Parameters
- input_size: 입력  𝑥
- hidden_size: 숨겨진 상태 ℎ의 특성 수입니다.
- num_layers: 반복 레이어의 수입니다. 예를 들어, num_layers=2로 설정하면 두 개의 LSTM을 쌓아서 쌓인 LSTM을 형성하게 되며, 두 번째 LSTM은 첫 번째 LSTM의 출력을 입력으로 받아 최종 결과를 계산합니다.
- bias: False로 설정하면 레이어가 편향 가중치 $$b_{ih}$$와 $$b_{hh}$$를 사용하지 않습니다.
- batch_first: True로 설정하면 입력 및 출력 텐서가 (배치, 시퀀스, 특성) 형태로 제공됩니다. 숨겨진 상태나 셀 상태에는 적용되지 않습니다. 자세한 내용은 입력/출력 섹션을 참조하십시오.
- dropout: 0이 아닌 값으로 설정하면, 각 LSTM 레이어의 출력에 Dropout 레이어를 추가합니다. 마지막 레이어를 제외한 모든 레이어에 적용되며, Dropout 확률은 dropout 값과 동일합니다.

## PyTorch Code
```python
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader, TensorDataset
from torch.nn.utils.rnn import pad_sequence
from keras.datasets import imdb
import matplotlib.pyplot as plt
import numpy as np

# IMDB 데이터셋을 로드합니다
(x_train, y_train), (x_test, y_test) = imdb.load_data(num_words=10000)

# 모든 시퀀스를 같은 길이로 패딩합니다
max_len = 500
x_train = pad_sequence([torch.tensor(seq) for seq in x_train], batch_first=True, padding_value=0)[:, :max_len]
x_test = pad_sequence([torch.tensor(seq) for seq in x_test], batch_first=True, padding_value=0)[:, :max_len]

y_train = torch.tensor(y_train)
y_test = torch.tensor(y_test)

# 데이터 로더를 만듭니다
batch_size = 32
train_data = TensorDataset(x_train, y_train)
test_data = TensorDataset(x_test, y_test)
train_loader = DataLoader(train_data, shuffle=True, batch_size=batch_size)
test_loader = DataLoader(test_data, shuffle=False, batch_size=batch_size)

# 모델을 정의합니다
class LSTMModel(nn.Module):
    def __init__(self, vocab_size, embed_size, hidden_size):
        super(LSTMModel, self).__init__()
        self.embedding = nn.Embedding(vocab_size, embed_size)
        self.lstm = nn.LSTM(embed_size, hidden_size, batch_first=True, dropout=0.2)
        self.fc = nn.Linear(hidden_size, 1)
        self.sigmoid = nn.Sigmoid()
    
    def forward(self, x):
        x = self.embedding(x)
        x, _ = self.lstm(x)
        x = self.fc(x[:, -1, :])
        return self.sigmoid(x)

# 하이퍼파라미터 설정
vocab_size = 10000
embed_size = 128
hidden_size = 128

# 모델 초기화
model = LSTMModel(vocab_size, embed_size, hidden_size)
criterion = nn.BCELoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)

# 모델을 학습합니다
num_epochs = 10
train_losses = []
val_losses = []
train_accuracies = []
val_accuracies = []

for epoch in range(num_epochs):
    model.train()
    train_loss = 0
    train_correct = 0
    for x_batch, y_batch in train_loader:
        optimizer.zero_grad()
        output = model(x_batch)
        loss = criterion(output.squeeze(), y_batch.float())
        loss.backward()
        optimizer.step()
        
        train_loss += loss.item()
        predicted = (output.squeeze() >= 0.5).float()
        train_correct += (predicted == y_batch).sum().item()
    
    train_loss /= len(train_loader)
    train_accuracy = train_correct / len(train_loader.dataset)
    train_losses.append(train_loss)
    train_accuracies.append(train_accuracy)
    
    model.eval()
    val_loss = 0
    val_correct = 0
    with torch.no_grad():
        for x_batch, y_batch in test_loader:
            output = model(x_batch)
            loss = criterion(output.squeeze(), y_batch.float())
            val_loss += loss.item()
            predicted = (output.squeeze() >= 0.5).float()
            val_correct += (predicted == y_batch).sum().item()
    
    val_loss /= len(test_loader)
    val_accuracy = val_correct / len(test_loader.dataset)
    val_losses.append(val_loss)
    val_accuracies.append(val_accuracy)
    
    print(f'Epoch {epoch+1}/{num_epochs}, Train Loss: {train_loss:.4f}, Train Accuracy: {train_accuracy:.4f}, Val Loss: {val_loss:.4f}, Val Accuracy: {val_accuracy:.4f}')

# 손실 값과 정확도 결과를 플롯합니다
plt.plot(train_losses, label='Train Loss')
plt.plot(val_losses, label='Validation Loss')
plt.title('Model Loss')
plt.ylabel('Loss')
plt.xlabel('Epoch')
plt.legend(loc='upper right')
plt.show()

plt.plot(train_accuracies, label='Train Accuracy')
plt.plot(val_accuracies, label='Validation Accuracy')
plt.title('Model Accuracy')
plt.ylabel('Accuracy')
plt.xlabel('Epoch')
plt.legend(loc='lower right')
plt.show()
```

## Referene
문서링크 : [https://bit.ly/3KYQWIC](https://bit.ly/3KYQWIC)

***
    🌜 개인 공부 기록용 블로그입니다. 오류나 틀린 부분이 있을 경우 
    언제든지 댓글 혹은 메일로 지적해주시면 감사하겠습니다! 😄


[맨 위로 이동하기](#){: .btn .btn--primary }{: .align-right}

