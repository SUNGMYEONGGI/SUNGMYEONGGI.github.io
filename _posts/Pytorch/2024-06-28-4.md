---
title:  "[PyTorch] torch.nn.Conv2d 관련 공식문서 정리"
excerpt: "torch.nn.Conv2d"

categories:
  - Pytorch
tags:
  - [torch.nn, Conv2d]
toc: true
toc_sticky: true
 
date: 2024-06-28
last_modified_at: 2024-06-28
---

# Conv2d
> CLASS torch.nn.Conv2d(in_channels, out_channels, kernel_size, stride=1, padding=0, dilation=1, groups=1, bias=True, padding_mode='zeros', device=None, dtype=None)

여러 입력 평면으로 구성된 입력 신호에 2D 컨볼루션을 적용합니다.

가장 간단한 경우, 입력 크기 $$(N, C_{in}, H, W)$$와 출력 크기 
$$(N, C_{out}, H_{out}, W_{out})$$인 레이어의 출력 값은 다음과 같이 정확하게 설명할 수 있습니다:

$$
\text{out}(N_i, C_{out_j}) = \text{bias}(C_{out_j}) + \sum_{k=0}^{C_{in}-1} \text{weight}(C_{out_j}, k) \ast \text{input}(N_i, k)
$$

$$N$$은 배치 크기(batch size)를 의미하고, $$C$$는 채널 수를 나타내며, $$H$$는 입력 평면의 높이를 픽셀 단위로 나타내고, $$W$$는 너비를 픽셀 단위로 나타냅니다.

- stride는 교차 상관을 위한 스트라이드를 제어하며, 단일 숫자 또는 튜플일 수 있습니다.
- padding은 입력에 적용되는 패딩의 양을 제어합니다. 'valid', 'same' 문자열 또는 양쪽에 적용되는 암시적 패딩의 양을 나타내는 int/tuple일 수 있습니다.
- dilation은 커널 포인트 간의 간격을 제어합니다. à trous 알고리즘으로도 알려져 있습니다. 설명하기는 어렵지만, 이 링크에는 dilation이 무엇을 하는지에 대한 좋은 시각화가 있습니다.
- groups는 입력과 출력 간의 연결을 제어합니다. in_channels와 out_channels는 모두 groups로 나누어 떨어져야 합니다. 예를 들어,
  - groups=1일 때, 모든 입력이 모든 출력으로 컨볼루션됩니다.
  - groups=2일 때, 연산은 각 입력 채널의 절반을 보고 절반의 출력 채널을 생성하는 두 개의 컨볼루션 레이어가 나란히 있는 것과 동등하게 됩니다. 그 후, 두 결과가 연결됩니다.
  - groups=`in_channels` 일때, 각 입력 채널은 고유한 필터 세트로 컨볼루션됩니다 (크기는 $$(\frac{out\_channels}{in\_channels})$$).

매개변수 `kernel_size`, `stride`, `padding`, `dilation`은 다음과 같을 수 있습니다:
- 단일 int – 이 경우 높이 및 너비 차원에 동일한 값이 사용됩니다.
- 두 개의 int 튜플 – 이 경우 첫 번째 int는 높이 차원에 사용되고, 두 번째 int는 너비 차원에 사용됩니다.

## Parameters
```python
nn.Conv2d(in_channels, out_channels, kernel_size, stride=1, padding=0, dilation=1, groups=1, bias=True)
```
- in_channels (int): 입력 채널의 개수. 예를 들어, RGB 이미지의 경우 3개의 채널을 가지므로 in_channels는 3이 됩니다.

  <details>
  <summary style="color:#FF0000;">in_channels 오류예시</summary>
  <div markdown="1">
  ```python
  # 샘플 이미지 구조의 텐서 생성
  BATCH_SIZE = 16
  sample = torch.randn(BATCH_SIZE, 3, 224, 224) # (채널, 높이, 너비)
  
  # ((I - 2xP + K )/ S) + 1  = O; ((224 - (2 x 0) + 5) / 1) + 1 = 220
  conv = nn.Conv2d(in_channels = 3, out_channels = 6, kernel_size = 5, stride = 1, padding = 0)
  conv(sample).shape # torch.Size([16, 6, 220, 220])
  ```

  ```python
  # 샘플 이미지 구조의 텐서 생성
  BATCH_SIZE = 16
  sample = torch.randn(BATCH_SIZE, 3, 224, 224) # (채널, 높이, 너비)
  
  # ((I - 2xP + K )/ S) + 1  = O; ((224 - (2 x 0) + 5) / 1) + 1 = 220
  conv = nn.Conv2d(in_channels = 1, out_channels = 6, kernel_size = 5, stride = 1, padding = 0)
  conv(sample).shape # torch.Size([16, 6, 220, 220])
  ```
  </div>
  ❌ 만약 텐서의 채널수와 in_channels 파라미터가 다르면 Runtime Error가 발생한다. ❌

  <img src="https://github.com/SUNGMYEONGGI/image/blob/main/%E1%84%89%E1%85%B3%E1%84%8F%E1%85%B3%E1%84%85%E1%85%B5%E1%86%AB%E1%84%89%E1%85%A3%E1%86%BA%202024-06-26%20%E1%84%8B%E1%85%A9%E1%84%92%E1%85%AE%204.30.36.png?raw=true" width="650px">
  </details>

- out_channels (int): 출력 채널의 개수, 즉 컨볼루션 필터의 개수입니다. 이 값이 클수록 더 복잡한 특징을 학습할 수 있지만, 모델의 파라미터 수가 증가하게 됩니다.
- kernel_size (int 또는 tuple): 컨볼루션 필터의 크기. 예를 들어, 3x3 필터의 경우 kernel_size는 3 또는 (3, 3)으로 지정할 수 있습니다.
- stride (int 또는 tuple, optional): 필터의 이동 간격, 즉 스트라이드(stride)입니다. 기본값은 1이며, 더 큰 값으로 설정하면 출력 특징 맵의 크기가 작아지게 됩니다.
- padding (int 또는 tuple, optional): 입력 데이터의 가장자리에 추가되는 패딩의 크기입니다. 기본값은 0이며, 패딩을 사용하면 출력 특징 맵의 크기를 보존하면서 입력 데이터의 가장자리 정보를 유지할 수 있습니다.
- dilation (int 또는 tuple, optional): 딜레이션(dilation) 레이트입니다. 딜레이션은 필터의 간격을 더 크게 두어 더 넓은 영역의 정보를 가져오는 데 사용됩니다. 기본값은 1이며, 값이 커질수록 필터의 영역이 더 넓어지게 됩니다.
- groups (int, optional): 입력 및 출력 채널을 묶는(grouping) 개수입니다. 기본값은 1이며, 값이 크면 채널 간의 관련성을 줄이는 효과가 있습니다.
- bias (bool, optional): 편향(bias)을 사용할지 여부를 결정하는 플래그입니다. 기본값은 True이며, False로 설정하면 편향이 사용되지 않습니다.

## Example Code
```python
import torch
import torch.nn as nn

# 입력 데이터의 크기: (배치 크기, 채널, 높이, 너비)
input_size = (64, 3, 32, 32)

# Conv2d 레이어 정의
conv = nn.Conv2d(in_channels=3, out_channels=64, kernel_size=3, stride=1, padding=1)

# 입력 데이터 생성
input_data = torch.randn(input_size)

# 컨볼루션 연산 수행
output = conv(input_data)

# 출력 데이터의 크기 출력
print("Output size:", output.size())

Output size: torch.Size([64, 64, 32, 32])
```

## Referene
문서링크 : [https://bit.ly/3RHruex](https://bit.ly/3RHruex)

관련영상 : [https://shorturl.at/Ta9ZO](https://shorturl.at/Ta9ZO)
***
    🌜 개인 공부 기록용 블로그입니다. 오류나 틀린 부분이 있을 경우 
    언제든지 댓글 혹은 메일로 지적해주시면 감사하겠습니다! 😄


[맨 위로 이동하기](#){: .btn .btn--primary }{: .align-right}